[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "deeptech_ipc_phase2"
version = "0.1.0"
description = "Phase-2 patent phrase mining pipeline"
requires-python = ">=3.10"
